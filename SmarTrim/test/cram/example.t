An example for cram tests.

Create a file:
  $ echo contents > data.txt

Display it:
  $ cat data.txt
  contents

Remove it:
  $ rm data.txt

Consecutive two commands concatenated with `;`:
  $ echo hello; false
  hello
  [1]
